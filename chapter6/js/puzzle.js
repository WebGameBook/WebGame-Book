var Puzzle = function (option) {
    this.x = option.x;
    this.y = option.y;
    this.value = option.value;
};

Puzzle.prototype.savePosition = function (position) {
};

Puzzle.prototype.updatePosition = function (position) {
};